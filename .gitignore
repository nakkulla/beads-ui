/node_modules
tsconfig.tsbuildinfo
.DS_Store
.beads/issues.jsonl
.worktrees/
.bdui/

# Dolt database files (added by bd init)
.dolt/
*.db

# Beads / Dolt files (added by bd init)
.beads-credential-key

# Runtime log (dolt server appends on every start) — never commit
.beads/dolt-config.log

# Brainstorming skill session scratch (mockups, pids, tokens)
.superpowers/
